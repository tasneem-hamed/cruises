{/*import React from "react";
//import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
//import { Button } from "@/components/ui/button";
import { Calendar, MapPin, Users, Ship } from "lucide-react";
import "./Destantios.css";

const BookingCard = ({ booking }) => {
  return (
    <Card className="overflow-hidden">
      <div
        className="h-40 bg-cover bg-center"
        style={{ backgroundImage: `url(${booking.image})` }}
      ></div>
      <CardHeader className="pb-2">
        <CardTitle>{booking.destination}</CardTitle>
      </CardHeader>
      <CardContent className="space-y-2">
        <div className="flex items-center text-sm">
          <Ship className="mr-2 h-4 w-4 text-gray-500" />
          <span>Cruise Booking #{booking.id}</span>
        </div>
        <div className="flex items-center text-sm">
          <Calendar className="mr-2 h-4 w-4 text-gray-500" />
          <span>{booking.departureDate} - {booking.returnDate}</span>
        </div>
        <div className="flex items-center text-sm">
          <Users className="mr-2 h-4 w-4 text-gray-500" />
          <span>{booking.guests} Guests • {booking.roomType}</span>
        </div>
        <div className="flex justify-between items-center mt-4 pt-2 border-t">
          <span className="font-bold">${booking.totalPrice}</span>
          <Button variant="outline" size="sm">View Details</Button>
        </div>
      </CardContent>
    </Card>
  );
};

export default BookingCard;
*/}